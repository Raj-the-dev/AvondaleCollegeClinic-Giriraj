@model AvondaleCollegeClinic.Models.Caregiver
@{
    ViewData["Title"] = "Delete Caregiver";
}

<section class="wrap" style="padding:36px 0">
    <div class="eyebrow">CAREGIVERS</div>
    <h1>Delete</h1>
    <p class="muted" style="margin-top:6px">Are you sure you want to delete this caregiver?</p>

    <div class="tile span-12" style="padding:26px; margin-top:12px">
        <dl class="row" style="margin:0">
            <dt class="col-sm-3 muted">Name</dt>
            <dd class="col-sm-9">@Model.FirstName @Model.LastName</dd>

            <dt class="col-sm-3 muted">Email</dt>
            <dd class="col-sm-9">@Model.Email</dd>

            <dt class="col-sm-3 muted">Phone</dt>
            <dd class="col-sm-9">@Model.Phone</dd>

            <dt class="col-sm-3 muted">Relationship</dt>
            <dd class="col-sm-9">@Model.Relationship</dd>

            @if (!string.IsNullOrEmpty(Model.ImagePath))
            {
                <dt class="col-sm-3 muted">Photo</dt>
                <dd class="col-sm-9"><img src="@Model.ImagePath" alt="Caregiver photo" width="120" height="120" /></dd>
            }
        </dl>

        <form asp-action="Delete" method="post" class="auth" style="max-width:560px; margin-top:16px">
            <input type="hidden" asp-for="CaregiverID" />
            <div class="form-row">
                <button type="submit" class="btn">Delete</button>
                <a asp-action="Index" class="link-quiet" style="margin-left:12px">Cancel</a>
            </div>
        </form>
    </div>
</section>
